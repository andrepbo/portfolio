/**
 * Interface representing the properties for a chip element.
 *
 * @interface ChipProps
 */
export default interface ChipProps {
  text: string; // The text content to be displayed in the chip.
}
