/**
 * Interface representing the properties for the content within a hero section.
 *
 * @interface HeroContentProps
 */
export default interface HeroContentProps {
  children: React.ReactNode; // The content to be displayed within the hero section.
}
