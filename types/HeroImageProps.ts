/**
 * Interface representing the properties for a hero image component.
 *
 * @interface HeroImageProps
 */
export default interface HeroImageProps {
  filename: string; // The filename of the hero image.
}
