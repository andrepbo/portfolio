/**
 * Interface representing the properties for a title within a hero section.
 *
 * @interface HeroTitleProps
 */
export default interface HeroTitleProps {
  text: string; // The text content of the title.
}
