/**
 * Interface representing the properties for a subtitle within a hero section.
 *
 * @interface HeroSubtitleProps
 */
export default interface HeroSubtitleProps {
  text: string; // The text content of the subtitle.
}
