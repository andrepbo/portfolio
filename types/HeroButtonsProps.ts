/**
 * Interface representing the properties for a container of hero buttons.
 *
 * @interface HeroButtonsProps
 */
export default interface HeroButtonsProps {
  children: React.ReactNode; // The content to be displayed within the container.
}
